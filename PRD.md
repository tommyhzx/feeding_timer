# 悦马喂养助手 (Joyous Horse Feeding Timer) - 产品设计文档 V1.0

## 1. 项目背景
本产品是专为 2026 马年新生儿父母设计的智能硬件，旨在解决新手父母在睡眠不足、精神疲惫状态下，难以准确记录喂奶间隔、喂奶时长及喂奶侧边的痛点。

## 2. 硬件架构
- **控制器**: ESP32-S3 (支持 Wi-Fi/蓝牙，具备强大处理能力)
- **显示屏**: MAX7219 8x16 LED 点阵模块
- **交互**: 2个物理按键 (Mode 键, Enter 键)
- **外壳**: 立方体结构，顶部安装 PCB，正面嵌入显示屏

## 3. 功能模式设计
产品采用状态机逻辑，通过 Mode 键在以下模式间循环切换：

### 3.1 喂养计时模式 (Feeding Timer)
- **显示**: `MM:SS` (分:秒) 或 `HH:MM` (小时:分钟)
- **逻辑**: 
    - 短按 Enter：开始计时/暂停计时。
    - 长按 Enter (2秒)：计时归零，并触发数据上传。
- **目的**: 记录喂奶持续时长，并自动计算距离上次喂奶的时间间隔。

### 3.2 左右侧记录模式 (Side Indicator)
- **显示**: `L` (左侧) 或 `R` (右侧)
- **逻辑**:
    - 短按 Enter：在 L 和 R 之间切换。
- **目的**: 辅助记忆上次喂奶的侧边，防止因疲劳导致的涨奶不均。

### 3.3 智能时钟模式 (Smart Clock)
- **显示**: `HH:MM` (24小时制)
- **逻辑**: 通过 ESP32-S3 的 Wi-Fi 连接 NTP 服务器自动同步北京时间。
- **目的**: 提供精准的夜间时间参考。

## 4. 交互逻辑 (UI/UX)
| 按键 | 操作 | 功能说明 |
| :--- | :--- | :--- |
| **Mode** | 短按 | 在 计时 -> 记录 -> 时钟 模式间循环切换 |
| **Mode** | 长按 | 进入系统设置 (如 Wi-Fi 配网) |
| **Enter** | 短按 | 执行当前模式的主要动作 (开始/切换) |
| **Enter** | 长按 | 重置当前状态或确认提交数据 |

## 5. 进阶功能 (IoT & Intelligence)
- **数据自动同步**: 每次计时结束并重置时，ESP32-S3 自动通过 REST API 或 MQTT 将数据发送至后端服务器。
- **夜间护眼模式**: 系统根据当前时间 (20:00 - 07:00) 自动将 MAX7219 亮度调节至最低等级。
- **低功耗优化**: 长时间无操作后自动进入深睡眠，通过按键中断唤醒。

## 6. 结构设计参考
- **外观**: 立方体外壳。
- **布局**: 顶部面放置 PCB 板及功能按键；正前方安装 MAX7219 显示屏；建议增加黑色半透明滤光片以提升显示质感。
